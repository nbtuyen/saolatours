expand_filter();	
function expand_filter(){
	$('.click-mobile-amp').click(function(e){
		$('#hellosssssss').toggleClass( "hide" );
		// var id = $(this).attr('data-id');
		// $( this ).toggleClass( "active" );
		// $('#'+id).toggle("slow");
	});
}